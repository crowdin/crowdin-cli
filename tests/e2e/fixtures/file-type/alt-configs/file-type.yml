project_id: "{{projectId}}"
api_token: "{{token}}"
base_path: "./files"
base_url: "{{baseUrl}}"
preserve_hierarchy: true
files:
  - source: "/android.xml"
    translation: "/android%two_letters_code%.xml"
    type: "{{type}}"
