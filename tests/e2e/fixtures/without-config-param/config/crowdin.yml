# Moved between crowdin.yml and crowdin.yaml by the suite to test default config discovery.
project_id: "{{projectId}}"
api_token: "{{token}}"
base_path: "."
base_url: "{{baseUrl}}"
preserve_hierarchy: false

files:
  - source: "/sources/*.xml"
    translation: "/translations/%two_letters_code%/%original_file_name%"
