project_id: "{{projectId}}"
api_token: "{{token}}"
base_path: "."
base_url: "{{baseUrl}}"
preserve_hierarchy: false
files:
  - source: "/folder_2/*.xml"
    translation: "/%locale%/folder_2/%file_name%.xml"
