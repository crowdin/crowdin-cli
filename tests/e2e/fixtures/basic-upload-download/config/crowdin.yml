project_id: "{{projectId}}"
api_token: "{{token}}"

base_path: "."
base_url: "{{baseUrl}}"
preserve_hierarchy: true

files:
  - source: "sources/*.md"
    translation: "translations/%locale%/%original_file_name%"
