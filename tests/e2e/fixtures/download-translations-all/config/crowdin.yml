project_id: "{{projectId}}"
api_token: "{{token}}"
base_path: "files"
base_url: "{{baseUrl}}"
preserve_hierarchy: true
files:
  - source: "/root/**/*.xml"
    translation: "/translations/%two_letters_code%/**/%file_name%.xml"
