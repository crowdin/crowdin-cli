import type { OptionDef } from '../../../types.ts';

export default {
  name: 'no-progress',
  type: 'boolean',
  description: 'Disable progress on executed command',
  default: false,
} as OptionDef;
