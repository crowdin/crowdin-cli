import type { OptionDef } from '../../../types.ts';

export default {
  name: 'token',
  short: 'T',
  type: 'string',
  description: 'Personal access token required for authentication',
} as OptionDef;
