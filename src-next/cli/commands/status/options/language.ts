import type { OptionDef } from '../../../types.ts';

export default {
  name: 'language',
  short: 'l',
  type: 'string',
  default: 'all',
  description: 'View progress for a single specified language',
} as OptionDef;
